import { makeStyles } from '@material-ui/styles';

export default makeStyles({
  btn: {
    color: 'white',
    backgroundColor: 'black',
    borderRadius: 0,
    height: '1.875rem',
    marginRight: '0.5rem',
  },
  icon: {
    fontSize: '1.275rem',
  }
});